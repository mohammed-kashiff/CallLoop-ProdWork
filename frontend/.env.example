# Optional. Leave unset for local demo (http://localhost:8000).
VITE_API_URL=http://localhost:8000

# Supabase Auth (CL-8). Anon key is public by design; still do not commit production values.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
