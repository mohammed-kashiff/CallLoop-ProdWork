Call Loop UI lives here (Vite + React + TypeScript).

From this folder:

```bash
npm install
npm run dev
```

API base defaults to `http://localhost:8000`. Override with `VITE_API_URL` in `frontend/.env` (see `.env.example`).

Full install, env, and smoke checklist: [../README.md](../README.md).
